$(".dynamic_subject").html('<%= label_tag :subjects %>:<br><% unless @previous_subjects.blank? %><% @previous_subjects.each do |subject| %><% subjects = Subject.find_by_id(subject)%><%= label_tag subjects.subject_name %>:<%= check_box_tag "registration[subject_id][]", subjects.id, true %><br/><% end %><% end %><% unless @subject_options.blank? %><% @subject_options.each do |subject| %><% subjects = Subject.find_by_id(subject)%><%unless @previous_subjects.include?(subjects.id) %><%= label_tag subjects.subject_name %>:<%= check_box_tag "registration[subject_id][]", subjects.id %><br/><% end %><% end %><% end %>');